// import express from "express";
// import {addTherapistWithImage,getAllTherapists,getTherapistById,updateTherapistById,deleteTherapistById} from "../Controllers/therapist.js"
// import { uploadTharapistImage } from "../Middlewares/multer.js";

// const router = express.Router();

// // ✅ Add therapist with uploaded image
// router.post("/add", uploadTharapistImage, addTherapistWithImage);

// // ✅ Get all therapists
// router.get("/all", getAllTherapists);

// // ✅ Get a single therapist by ID
// router.get("/:id", getTherapistById);

// // ✅ Update therapist details by ID
// router.put("/:id", updateTherapistById);

// // ✅ Delete therapist and associated image by ID
// router.delete("/:id", deleteTherapistById);

// export default router;
